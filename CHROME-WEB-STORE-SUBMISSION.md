# HLS Ahead — Chrome Web Store 提交资料

更新日期：2026-07-31

> 本文按当前扩展 0.1.0 的实际代码填写。方括号内容必须由发布者替换。

配套的可直接粘贴资料：

- `STORE-LISTING.md`：简体中文和英文商店详情、分类与发布选项
- `STORE-PRIVACY-DISCLOSURES.md`：数据类型、数据使用认证、权限和远程代码说明
- `REVIEWER-INSTRUCTIONS.md`：审核人员测试步骤与联系信息模板
- `PRIVACY-POLICY-ZH.md`、`PRIVACY-POLICY-EN.md`：中英文隐私政策

本文保留完整发布流程、素材规格和正式送审检查清单；填写后台时应优先复制上述拆分文件中的对应字段。

## 1. 发布者账户

- 发布者名称：`[个人名或公司名]`
- 开发者联系邮箱：`[长期有效的邮箱]`
- 支持邮箱：`[支持邮箱]`
- 官网：`[https://你的域名/hls-ahead]`
- 支持页面：`[https://你的域名/hls-ahead/support]`
- 隐私政策：`[https://你的域名/hls-ahead/privacy]`
- 开启 Google 账户两步验证：是
- 完成 Chrome Web Store 开发者注册及一次性注册费：是
- 验证开发者邮箱：是

## 2. Store Listing / 商店详情

### 基本字段

- 名称：`HLS Ahead`
- 默认语言：`中文（简体）`
- 类别：`工具 / Tools`
- 成熟内容：`否`
- 官方网址：`[已通过 Google Search Console 验证的域名；没有可暂不填]`
- 首页网址：`[https://你的域名/hls-ahead]`
- 支持网址：`[https://你的域名/hls-ahead/support]`

### 简短说明（不超过 132 个字符）

检测当前页面正在播放的 HLS 清单，并预取后续媒体分片，减少网络波动造成的播放等待。

### 详细说明

HLS Ahead 会检测当前标签页正在使用的 HLS（m3u8）播放清单，并提前请求接下来可能播放的媒体分片，尝试利用浏览器临时缓存减少播放过程中的等待。

主要功能：

- 自动识别当前页面中的 m3u8/HLS 请求
- 自动解析主清单和媒体清单
- 默认选择最高码率线路，避免同时请求所有清晰度
- 支持分别设置预取分片数量、并发数和单个资源大小上限
- 显示最近发现的清单、预取数量、流量和失败状态
- 可对当前标签页随时暂停或重新开始预取

隐私与使用说明：

为了实现该功能，扩展会在用户设备本地读取页面发出的网络请求 URL、HLS 清单内容及请求所需的临时鉴权信息，并向原资源服务器请求媒体分片。这些信息不会发送给扩展开发者，不会用于广告、用户画像或分析，也不会出售或共享给无关第三方。扩展不会永久保存视频，不提供视频下载功能，也不会绕过 DRM、付费限制或网站访问控制。

预取效果取决于网站、播放器和资源服务器的缓存策略。使用 `Cache-Control: no-store`、一次性链接或特殊 DRM 的内容可能无法获得加速效果。请仅在你有权访问和播放相关内容的网站上使用。

### 推荐搜索概念（不要在描述中堆砌）

HLS、m3u8、视频缓冲、媒体预取、播放加速。

## 3. Privacy practices / 隐私字段

### Single purpose / 单一用途

检测当前标签页正在使用的 HLS 播放清单，并在原资源服务器上预取后续媒体分片，以减少播放缓冲等待。

### 权限用途说明

#### `storage`

仅在用户设备本地保存预取分片数量、并发数和单资源大小上限等扩展设置。不会用它保存视频内容、浏览历史或鉴权信息。

#### `webRequest`

用于识别当前标签页成功完成的 HLS/m3u8 网络请求、响应类型以及该播放请求所需的临时鉴权头。扩展只将这些信息用于解析清单和预取同一播放资源。

#### `scripting`

用于在当前标签页的页面网络上下文中发起预取请求，使请求能够沿用该页面的 Cookie、鉴权环境和浏览器缓存分区。扩展不会借此修改页面内容或注入广告。

#### `tabs`

当前 0.1.0 代码只使用活动标签页 ID 并在标签页关闭时清理临时状态。该权限并非必需，正式送审版应从 Manifest 中移除，而不是填写理由保留。

#### Host permission: `<all_urls>`

HLS 清单及媒体分片可能位于用户访问页面之外的任意 CDN 域名。该访问范围用于检测页面实际发生的 HLS 请求并向对应原资源服务器预取分片；扩展不会扫描未访问网站，也不会将 URL 或页面数据发送给开发者。

### Remote code / 远程代码

- 是否使用远程代码：`否`
- 说明：`所有 JavaScript 均包含在扩展安装包内。远程获取的 m3u8 清单和媒体分片仅作为数据处理，不会作为代码执行；扩展不使用 eval、远程脚本或 WebAssembly。`

### 需要勾选的数据类型

按当前代码的实际行为，建议披露以下类型：

- `Authentication information / 身份验证信息`：临时读取 Authorization、X-Auth-Token 或 X-API-Key，并只用于请求原资源服务器；不持久保存。
- `Web history / 网络历史记录`：处理当前页面请求的 m3u8 和媒体资源 URL；只在本地运行时内存中使用。
- `User activity / 用户活动`：功能需要监视当前标签页的网络请求活动；不记录点击、键盘输入或滚动行为。
- `Website content / 网站内容`：读取 m3u8 清单文本以找出后续媒体分片。

不勾选：个人身份信息、健康信息、财务及付款信息、私人通信、位置、用户生成内容（除非以后版本新增相应功能）。

### 数据使用说明

- 数据是否发送给开发者服务器：`否`
- 数据是否出售给第三方：`否`
- 数据是否用于或转移给与扩展单一用途无关的目的：`否`
- 数据是否用于信用、借贷或保险资格判断：`否`
- 数据是否用于个性化广告或再营销：`否`
- 是否允许开发者或其他人员人工读取用户数据：`否`
- 是否仅为实现公开说明的用户功能而使用数据：`是`
- 是否遵守 Chrome Web Store User Data Policy 的 Limited Use 要求：`是`

隐私政策 URL 必须公开、无需登录、使用 HTTPS，并与上面披露及扩展实际行为完全一致。

## 4. Distribution / 发布范围

首发建议：

- 可见性：`Unlisted / 不公开列出`，先完成真实用户测试；稳定后改为 `Public / 公开`
- 地区：`所有地区`
- 付费：`免费`
- 应用内购买：`无`
- 延迟发布：首版建议开启，审核通过后手动确认上线时间

如果同时发布测试版，名称末尾必须加 `BETA` 或 `DEVELOPMENT BUILD`，描述中明确写明测试版本。

## 5. Test instructions / 审核测试说明

无需账户或测试凭据。

1. 安装扩展并打开 `https://hlsjs.video-dev.org/demo/`。
2. 开始播放页面中的 HLS 示例视频。
3. 点击 Chrome 工具栏中的 HLS Ahead 图标。
4. 弹窗应显示“最近发现”中的 m3u8 URL，并进入“正在预拉取”或“预拉取完成”状态。
5. “已拉取”和“数据量”应增加；网络或跨域策略不允许时，“失败”计数可能增加并显示兼容模式。
6. 关闭弹窗中的开关后，当前标签页应显示“此页面已暂停”，且不再启动新的预取。
7. 在“预拉取设置”中修改分片数量并保存，再点击“再次预拉取最近清单”，新设置应生效。

审核人员不需要付费账户。扩展不会永久下载或导出媒体文件。

## 6. 图形素材清单

### 扩展安装包内（发布前本地生成）

- `icons/icon16.png`：16×16
- `icons/icon32.png`：32×32
- `icons/icon48.png`：48×48
- `icons/icon128.png`：128×128，PNG
- 二进制文件不进入 Git；使用 `store-assets/prompts.jsonl` 和 `gpt-image-2` 生成源图后，在发布工作区导出这些尺寸
- 导出后在发布包的 `manifest.json` 中通过 `icons` 和 `action.default_icon` 声明这些文件

128×128 商店图标建议将主要图形控制在 96×96，中间放置，四周各留约 16 px 透明空间，并确保浅色和深色背景均清晰。

### 商店后台素材（发布前本地生成）

- 使用 `store-assets/prompts.jsonl` 中的 `gpt-image-2` 提示词生成 4 张截图源图和宣传源图
- 将 4 张截图裁剪或缩放为 1280×800
- 将宣传源图分别导出为 440×280 小型宣传图和 1400×560 Marquee 图
- YouTube 演示视频：可选

### 建议的 5 张截图内容

1. 视频播放页面 + 扩展发现 m3u8 的弹窗，标题：“自动发现 HLS 播放清单”
2. 成功预取后的统计界面，标题：“查看预取数量与流量”
3. 设置展开状态，标题：“控制分片数量和并发”
4. 当前页面暂停状态，标题：“按标签页随时启停”
5. 跨域兼容模式状态，标题：“适配不同 HLS 播放环境”

图片只能展示扩展真实存在的界面和能力；不要写“最快”“100% 加速”“官方推荐”等无法证明的表述。

## 7. 正式送审前必须处理

### 阻塞项

- [ ] 使用 `gpt-image-2` 生成图标源图，并在发布工作区导出 16/32/48/128 PNG 图标。
- [ ] 在发布包 Manifest 中声明 `icons` 和 `action.default_icon`。
- [ ] 删除当前不必要的 `tabs` 权限，满足最小权限原则。
- [ ] 将隐私政策发布到公开 HTTPS URL，并替换所有发布者/邮箱/URL 占位符。
- [ ] 使用 `gpt-image-2` 生成并人工复核 4 张功能截图、440×280 小型宣传图和 1400×560 Marquee 图。
- [ ] 在真实 Chrome 中对普通 HLS、跨域 HLS、直播、点播、暂停开关和设置持久化进行人工测试。

### 强烈建议

- [ ] 在弹窗内增加一行始终可见的隐私提示：“仅在本机处理播放请求，不向开发者上传数据。”
- [ ] 增加隐私政策和支持页面入口。
- [ ] 对非 HTTP(S) 页面、Chrome 内部页面和脚本注入失败提供清晰提示。
- [ ] 首先以 Unlisted 发布给测试用户，再转 Public。
- [ ] 避免在商店文案中使用“下载器”“破解”“绕过 DRM”等措辞；产品能力应始终定位为临时预取和播放缓冲优化。

## 8. 上传与发布顺序

1. 注册并设置开发者账户，验证邮箱，开启两步验证。
2. 完成上述阻塞项，提升 Manifest 版本号。
3. 将 `manifest.json` 放在 ZIP 根目录后重新打包。
4. 在 Developer Dashboard 点击 Add new item 并上传 ZIP。
5. 填写 Store Listing、Privacy、Distribution 和 Test instructions。
6. 检查商店披露、隐私政策和代码行为三者完全一致。
7. 提交审核；首发选择延迟发布或 Unlisted。
