# HLS Ahead 隐私政策

生效日期：2026 年 7 月 31 日

HLS Ahead（以下简称“本扩展”）用于检测当前标签页正在使用的 HLS（m3u8）播放清单，并预取后续媒体分片，以尝试减少视频播放过程中的缓冲等待。

发布者：`[发布者个人名或公司名]`  
联系邮箱：`[隐私联系邮箱]`

## 1. 本扩展处理的信息

为了提供上述功能，本扩展可能在用户设备本地处理：

- 当前标签页发出的 HLS/m3u8 及相关媒体资源 URL；
- m3u8 播放清单的文本内容；
- 与原播放请求关联、且预取同一资源所必需的临时身份验证请求头，例如 Authorization、X-Auth-Token 或 X-API-Key；
- 当前标签页中的网络请求活动；
- 用户设置的预取分片数量、并发数和单资源大小上限；
- 预取成功数、失败数和流量等临时运行状态。

本扩展不会读取表单内容、键盘输入、私人通信、付款信息、健康信息或精确位置。

## 2. 信息的使用方式

上述信息仅用于：

- 识别 HLS 播放清单；
- 解析清单中的后续媒体资源；
- 向内容原本所在的资源服务器发起预取请求；
- 在扩展弹窗中显示本地运行状态；
- 保存用户选择的扩展设置。

本扩展不会利用这些信息进行广告投放、跨站跟踪、用户画像、信用或保险资格判断，也不会将其出售。

## 3. 数据传输和共享

本扩展没有开发者运营的数据收集服务器，也不会把页面 URL、播放清单、身份验证信息或运行统计上传给扩展开发者。

为了完成用户请求的预取，本扩展会向视频内容原本所在的服务器或 CDN 发起网络请求。这些请求可能沿用当前页面已有的 Cookie 或临时身份验证信息。此类传输仅发生在提供用户所请求功能所必要的范围内，接收方是用户正在访问的服务及其内容分发提供商，其数据处理还可能受对应网站自身隐私政策约束。

除法律要求、保护用户和服务安全，或在取得适用法律要求的同意后发生业务转让外，发布者不会向其他第三方披露用户数据。

## 4. 保存期限

- m3u8 URL、临时鉴权头和运行统计仅保存在扩展运行内存中。临时鉴权头会在对应网络请求完成或失败后删除；标签页状态会在标签页关闭或扩展后台被浏览器回收时清除。
- 预取设置通过 Chrome 的本地存储保存在用户设备上，直至用户修改设置、清除扩展数据或卸载本扩展。
- 预取的媒体资源可能进入 Chrome 管理的 HTTP 临时缓存，其保存时间由浏览器和资源服务器的缓存策略决定。发布者不能访问该缓存。

## 5. 数据安全

本扩展不会永久保存身份验证信息。所有扩展代码均随安装包提供，不会下载并执行远程代码。本扩展只按原资源 URL 使用 HTTP 或 HTTPS；用户应仅在可信网站上使用本扩展。对于个人或敏感信息，本扩展仅应通过受现代加密保护的连接进行传输。

## 6. 用户的选择和控制

用户可以：

- 在弹窗中暂停当前标签页的预取；
- 调整预取数量、并发和资源大小限制；
- 关闭相关标签页以清除该标签页的临时运行状态；
- 卸载本扩展，以停止所有处理并删除 Chrome 为扩展保留的本地设置。

如需提出隐私问题或适用法律下的数据权利请求，请联系 `[隐私联系邮箱]`。由于发布者不接收用户的浏览或播放数据，通常没有可由发布者检索或删除的此类服务器端数据。

## 7. 儿童隐私

本扩展不以儿童为目标，也不会故意收集儿童的个人信息。如果你认为发布者意外收到了儿童个人信息，请通过上述邮箱联系。

## 8. Chrome Web Store Limited Use

本扩展对从 Chrome API 获得的信息的使用遵守 Chrome Web Store User Data Policy，包括 Limited Use 要求。本扩展仅将相关信息用于提供或改进公开说明的单一用户功能，不将其用于个性化广告，也不允许人员读取用户数据，但用户明确同意的支持、安全调查、法律要求或政策允许的聚合内部运营情形除外。

## 9. 政策更新

如果本扩展的数据处理方式发生变化，发布者会更新本政策、Chrome Web Store 隐私披露，并在政策或法律要求时于扩展界面中作出醒目说明及征求同意。生效日期会同步更新。

